version: "3.9"

services:
  playlist-db:
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=fakepassword
      - POSTGRES_USER=admin
      - POSTGRES_DB=playlist
